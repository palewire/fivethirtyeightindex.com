node_modules/
.svelte-kit/
build/
.lighthouseci/
.env
.env.*
!.env.example
*.log
.DS_Store
# The data file is checked in so GitHub Pages has data to deploy.
# Regenerate it locally with `make site-data` after re-running enrich.
